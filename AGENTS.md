# Public repository guidance

Keep this repository documentation-only. Update README, changelog, roadmap, command, configuration, webhook, deployment, and release-index Markdown whenever deployable behavior changes. Never add private source, secrets, server configuration values, or runtime data.